eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://118.31.20.172:7999/eureka,http://116.62.196.53:7999/eureka}
    prefer-same-zone-eureka: true
  instance:
    appname: fota-trade
spring:
  application:
    name: fota-trade
  cloud:
    config:
      profile: ${SPRING_PROFILE:dev}
      label: master
      discovery:
        enabled: true
        service-id: fota-config-server

