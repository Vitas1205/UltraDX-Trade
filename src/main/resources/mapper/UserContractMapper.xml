<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.fota.trade.mapper.UserContractMapper">
    <resultMap id="BaseResultMap" type="com.fota.trade.domain.UserContractDO" >
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="user_id" property="userId" jdbcType="BIGINT" />
        <result column="amount" property="amount" jdbcType="DECIMAL" />
        <result column="locked_amount" property="lockedAmount" jdbcType="DECIMAL" />
        <result column="gmt_create" property="gmtCreate" jdbcType="TIMESTAMP" />
        <result column="gmt_modified" property="gmtModified" jdbcType="TIMESTAMP" />
        <result column="version" property="version" jdbcType="INTEGER" />
        <result column="status" property="status" jdbcType="INTEGER" />
    </resultMap>
    <sql id="Base_Column_List" >
        id,user_id,amount,locked_amount,gmt_create,gmt_modified,version,status
    </sql>

    <insert id="insert" parameterType="com.fota.trade.domain.UserContractDO">
        INSERT INTO user_contract(user_id, gmt_create, gmt_modified, version)
        VALUES (#{userId},now(),now(),#{version})
    </insert>


    <select id="getByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM user_contract
        WHERE user_id = #{userId}
    </select>

    <update id="updateBalanceUnlimit">
        update user_contract
        set gmt_modified = now(), amount = amount + #{addAmount}
        where user_id = #{userId}
    </update>


</mapper>